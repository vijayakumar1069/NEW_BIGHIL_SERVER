<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Contact Inquiry - Bighil Platform</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: #f7f9f8;
        color: #242e4c;
        line-height: 1.6;
        padding: 20px;
        position: relative;
      }

      .email-container {
        max-width: 800px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(25, 135, 84, 0.1);
        overflow: hidden;
        border: 1px solid #dce6de;
      }

      .header {
        background: #198754;
        color: #ffffff;
        padding: 20px 15px;
        text-align: center;
        position: relative;
      }

      .header::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 20"><defs><pattern id="grain" width="100" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="white" opacity="0.1"/><circle cx="50" cy="5" r="0.5" fill="white" opacity="0.1"/><circle cx="80" cy="15" r="0.8" fill="white" opacity="0.1"/></pattern></defs><rect width="100" height="20" fill="url(%23grain)"/></svg>');
        opacity: 0.1;
      }

      .logo-placeholder {
        width: 80px;
        height: 80px;
        background: rgba(255, 255, 255, 0.2);
        border: 2px dashed rgba(255, 255, 255, 0.5);
        border-radius: 50%;
        margin: 0 auto 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: rgba(255, 255, 255, 0.8);
        font-size: 12px;
        position: relative;
        z-index: 1;
      }

      .header h1 {
        font-size: 32px;
        font-weight: 700;
        margin-bottom: 8px;
        position: relative;
        z-index: 1;
      }

      .header .subtitle {
        font-size: 16px;
        opacity: 0.9;
        font-weight: 300;
        position: relative;
        z-index: 1;
      }

      .content {
        padding: 40px 30px;
      }

      .inquiry-badge {
        display: inline-block;
        background: #d1e7dd;
        color: #198754;
        padding: 8px 20px;
        border-radius: 25px;
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 30px;
        border: 1px solid #a3cfbb;
      }

      .company-header {
        background: #f4fdf6;
        padding: 25px;
        border-radius: 12px;
        margin-bottom: 30px;
        border-left: 5px solid #198754;
        position: relative;
      }

      .company-name {
        font-size: 28px;
        font-weight: 700;
        color: #198754;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .company-name::before {
        content: "🏢";
        font-size: 24px;
      }

      .employee-count {
        background: #37a367;
        color: white;
        padding: 6px 15px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 600;
        display: inline-block;
        margin-top: 8px;
      }

      .details-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 25px;
        margin-bottom: 30px;
      }

      .detail-card {
        background: #ffffff;
        border: 1px solid #eff6f1;
        border-radius: 12px;
        padding: 20px;
        transition: all 0.3s ease;
        box-shadow: 0 2px 8px rgba(25, 135, 84, 0.05);
      }

      .detail-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(25, 135, 84, 0.1);
      }

      .detail-label {
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: #6c757d;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .detail-value {
        font-size: 16px;
        font-weight: 500;
        color: #242e4c;
        word-break: break-word;
      }

      .email-value {
        color: #37a367;
        text-decoration: none;
      }

      .email-value:hover {
        text-decoration: underline;
      }

      .message-section {
        background: #f4fdf6;
        border: 1px solid #eff6f1;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 30px;
      }

      .message-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 15px;
        padding-bottom: 15px;
        border-bottom: 2px solid #eff6f1;
      }

      .message-header h3 {
        color: #198754;
        font-size: 20px;
        font-weight: 600;
      }

      .subject-line {
        background: #37a367;
        color: white;
        padding: 12px 20px;
        border-radius: 8px;
        margin-bottom: 20px;
        font-weight: 500;
        border-left: 4px solid #198754;
      }

      .message-content {
        font-size: 16px;
        line-height: 1.8;
        color: #242e4c;
        background: white;
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #eff6f1;
        min-height: 120px;
      }

      .contact-preferences {
        background: #ffffff;
        border-radius: 12px;
        padding: 25px;
        border: 1px solid #dee2e6;
      }

      .contact-preferences h3 {
        color: #000000;
        font-size: 18px;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .contact-preferences h3::before {
        content: "📅";
        font-size: 18px;
      }

      .preference-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 0;
        border-bottom: 1px solid #dee2e6;
      }

      .preference-item:last-child {
        border-bottom: none;
      }

      .preference-label {
        font-weight: 600;
        color: #000000;
      }

      .preference-value {
        /* background: #a3cfbb; */
        color: #000000;
        padding: 6px 15px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 500;
      }

      .footer {
        background: #37a367;
        color: #ffffff;
        padding: 30px;
        text-align: center;
      }

      .footer p {
        margin-bottom: 10px;
        opacity: 0.8;
      }

      .timestamp {
        background: rgba(255, 255, 255, 0);
        padding: 8px 16px;
        border-radius: 6px;
        font-size: 14px;
        display: inline-block;
        margin-top: 10px;
      }

      /* Icons using CSS */
      .icon-email::before {
        content: "📧";
        margin-right: 6px;
      }
      .icon-calendar::before {
        content: "📅";
        margin-right: 6px;
      }
      .icon-clock::before {
        content: "🕐";
        margin-right: 6px;
      }
      .icon-message::before {
        content: "💬";
        margin-right: 6px;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        body {
          padding: 10px;
        }
        .email-container {
          margin: 0;
        }
        .header,
        .content,
        .footer {
          padding: 20px;
        }
        .details-grid {
          grid-template-columns: 1fr;
          gap: 15px;
        }
        .company-name {
          font-size: 24px;
        }
        .header h1 {
          font-size: 28px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <!-- Header Section -->
      <div class="header">
        <div class="logo-placeholder">
          <img src="<%= imagePath %>" alt="Logo" style="height: 50px" />
        </div>
        <h1>Bighil Platform</h1>
        <p class="subtitle">New Client Request Received</p>
      </div>

      <!-- Content Section -->
      <div class="content">
        <div class="inquiry-badge">🚀 New Client Contact Request</div>

        <!-- Company Information -->
        <div class="company-header">
          <div class="company-name"><%= companyName %></div>
          <div class="employee-count">
            👥 <%= numberOfEmployees %> Employees
          </div>
        </div>

        <!-- Contact Details Grid -->
        <div class="details-grid">
          <div class="detail-card">
            <div class="detail-label icon-email">Company Email</div>
            <div class="detail-value">
              <a href="mailto:<%= companyEmail %>" class="email-value"
                ><%= companyEmail %></a
              >
            </div>
          </div>
          <div class="detail-card">
            <div class="detail-label icon-calendar">Preferred Contact Date</div>
            <div class="detail-value"><%= bestContactDate %></div>
          </div>
        </div>

        <!-- Message Section -->
        <div class="message-section">
          <div class="message-header">
            <h3 class="icon-message">Message Details</h3>
          </div>
          <div class="subject-line">
            <strong>Subject:</strong> <%= subject %>
          </div>
          <div class="message-content"><%= message %></div>
        </div>

        <!-- Contact Preferences -->
        <div class="contact-preferences">
          <h3>Contact Preferences</h3>
          <div class="preference-item">
            <span class="preference-label icon-calendar">Preferred Date</span>
            <span class="preference-value"><%= bestContactDate %></span>
          </div>
          <div class="preference-item">
            <span class="preference-label icon-clock">Preferred Time</span>
            <span class="preference-value"><%= bestContactTime %></span>
          </div>
        </div>
      </div>

      <!-- Footer Section -->
      <div class="footer">
        <p><strong>Bighil Platform Administration</strong></p>
        <p>
          This inquiry was automatically generated from your platform contact
          form.
        </p>
        <div class="timestamp">
          Received: <%= new Date().toLocaleString() %>
        </div>
      </div>
    </div>
  </body>
</html>
